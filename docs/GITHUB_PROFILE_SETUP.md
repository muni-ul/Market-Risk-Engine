# GitHub Profile Setup

Use this page when polishing the repository for recruiter review. These settings
do not change the code; they make the GitHub project card and repository header
communicate the right software-engineering signal quickly.

## Repository Description

Suggested GitHub About description:

```text
Local Python simulation engine for options pricing, risk validation, benchmark reporting, and reproducible CSV/PNG/Markdown artifacts.
```

## Repository Topics

Suggested topics:

```text
python
simulation
cli
numpy
pandas
scipy
matplotlib
pytest
benchmarking
risk-validation
automation
testing
performance
reproducibility
portfolio-project
```

## Website Field

If you add a portfolio page later, use that URL. Until then, leave the website
field blank rather than linking to a generated `results/` artifact that only
exists locally.

## Pinned Repository Blurb

Suggested pinned-repo caption:

> Local Python simulation and reporting engine with deterministic configs,
> NumPy/SciPy numerical code, pytest coverage, risk validation, benchmark
> evidence, and reproducible CSV/PNG/Markdown outputs.

## Presentation Checks

- Keep the repository public only after your final local validation pass.
- Pin the repository near the top of your GitHub profile when applying.
- Keep `.github/CODEOWNERS` aligned with the GitHub account that owns the repo.
- Do not describe the project as live trading, brokerage-connected,
  investment advice, or a profitability model.
- Use `docs/FINAL_REVIEW_CHECKLIST.md` before making the repository public or
  adding it to a resume.
- Use `docs/RECRUITER_BRIEF.md` for a one-page quick-skim summary.
- Use `docs/RESUME_SNIPPETS.md` for resume bullets that match the implemented
  project.
